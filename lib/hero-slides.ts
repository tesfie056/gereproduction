import type { GallerySlide } from "@/lib/gallery-items";
import { allProjectSlides } from "@/lib/gallery-items";

/**
 * Full-screen hero slider — same ordered media as Projects (`allProjectSlides`).
 */
export const heroSlides: GallerySlide[] = allProjectSlides;
